using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponStyleManager : MonoBehaviour
{//NON ACTIVE SCRIPT
    //From the prefab:
    public string MeleeType;  // 'dagger' 'sword' 'spear' etc.

    public GameObject MagicType; //magic prefab to pass to the Weapon class
    public GameObject MiscType;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
